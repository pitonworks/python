"""
Constants - Oyun sabitleri
Oyun boyutları, yönler ve hücre boyutları burada tanımlanır.
"""

# Grid size - number of cells in the grid (20x20)
GRID_SIZE = 20

# Direction constants - (row change, column change) format
UP = (-1, 0)      # Up: row -1, column 0
DOWN = (1, 0)     # Down: row +1, column 0
LEFT = (0, -1)    # Left: row 0, column -1
RIGHT = (0, 1)    # Right: row 0, column +1

# Cell size - size of each grid cell in pixels
CELL_SIZE = 25
